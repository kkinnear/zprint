# argument 1 = location of graalvm for Linux
# argument 2 = file name of native-image .jar
# argument 2 = version of zprint (n.m.p) to build (must be target/zprint-filter-n.m.p)
docker run --rm -it -v `pwd`:/app/ danny02/graalvm bash /app/zprintl.sh $1 $2 $3
